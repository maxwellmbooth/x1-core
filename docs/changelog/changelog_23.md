# Changelog 23

- Added `rtl/ram_new.sv`: a draft of the data RAM module rewritten to use the generic request/response interface (matching the IMEM pattern), in preparation for replacing the current direct-wired RAM
